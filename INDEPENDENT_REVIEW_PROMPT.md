# Project 3 — Post-Merge Verification Prompt

Use this only after Project 3 has been merged to GitHub.

Repository:
`https://github.com/kalyansrinivas2k26/vantix-control-value`

Verify the live repository only. Do not redesign architecture.

Check:

1. Root README identity is `VANTIX Control Value — Customer Commitment Assurance`.
2. Genuine Control Value infrastructure remains present:
   - `contracts/v1.0.0/`
   - `fixtures/`
   - `governance/`
   - `package.json`
   - `package-lock.json`
   - Control Value build/validation/checksum scripts
   - `validation/`
   - `standards/`
   - Control Value evidence
3. Active Attestor Service Recovery and Customer Momentum artifacts are no longer Project 3 active scope.
4. Historical Attestor transition remains under `archive/attestor-transition/`.
5. `live-ai-diagnosis-preview.mjs` is not used to claim live Gemini execution unless real execution evidence exists.
6. Verify:
   - preserved npm validation pipeline;
   - repository validator;
   - graph validator;
   - exact-node offline tests;
   - checksum verification.
7. Confirm the exact GitHub Actions run for the merged commit is Green.
8. Check README/evidence links and scorecard arithmetic.
9. Report only real blockers.

Required final verdict:
- `PROJECT 3 — PASS / FREEZE`
- or `PROJECT 3 — HOLD` with the exact failed evidence/control.

Do not reopen architecture unless a concrete implementation defect is found.
